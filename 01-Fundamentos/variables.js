/*
Variables

Las variables son contenedores que almacenan datos.

Para usar vriables, se declaran usando las palabras reservadas:

var ->Es la forma tradicional. Pero ya no se recomienda.
let ->Es la forma moderna y la recomendada
const -> EL dato almacenado no puede cambiar.

Para declrar una variable:

var (nombre de la variable)= valor;

*/ 

var nombre ="Mike";
let edad= 21;
const vivo= true;

console.log(nombre);
console.log(edad);
console.log(vivo);

/*
Las variables declaradas con "var" y "let" pueden cambiar a lo largo del código siempre y cuando su alcance lo permita.

Las variables declradas con "const" no pueden cambiar.
Si intentamos marcara error.

para cabiar el valor de una variable:

(Nombre variable) = (nueva variable)
*/

nombre= "Matanga"
edad = 18

//Mostrar nuevos datos
console.log(nombre)
console.log(edad)
